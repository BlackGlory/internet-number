{
  "name": "internet-number",
  "version": "2.2.0",
  "description": "Utility for fethcing and parsing latest statistics files from RIR",
  "files": [
    "lib"
  ],
  "main": "lib/es2018/index.js",
  "types": "lib/es2018/index.d.ts",
  "repository": "git@github.com:BlackGlory/internet-number.git",
  "author": "BlackGlory <woshenmedoubuzhidao@blackglory.me>",
  "license": "MIT",
  "scripts": {
    "lint": "tslint -t codeFrame 'src/**/*.ts' '__tests__/**/*.ts'",
    "test": "jest --config jest.config.js",
    "test:debug": "node --inspect-brk node_modules/.bin/jest --runInBand",
    "coverage": "jest --coverage --config jest.config.js",
    "prepublishOnly": "yarn clean && yarn build",
    "clean": "rm -rf lib",
    "build": "yarn build:es2015 && yarn build:es2018",
    "build:es2015": "tsc --project tsconfig.build.json --target es2015 --outDir lib/es2015 && tscpaths -p tsconfig.build.json -s ./src -o ./lib/es2015",
    "build:es2018": "tsc --project tsconfig.build.json --target es2018 --outDir lib/es2018 && tscpaths -p tsconfig.build.json -s ./src -o ./lib/es2018"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint && yarn test"
    }
  },
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "@types/node": "^13.13.0",
    "@types/tmp": "^0.1.0",
    "extra-promise": "^0.5.0",
    "husky": "^4.2.5",
    "jest": "^25.3.0",
    "memfs": "^3.1.2",
    "nanoid": "^3.1.3",
    "ts-jest": "^25.4.0",
    "tscpaths": "^0.0.9",
    "tslint": "^6.1.1",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "get-uri": "^3.0.2",
    "md5-file": "^5.0.0"
  }
}
